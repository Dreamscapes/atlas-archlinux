--- pvmnet.c	2014-10-17 10:16:29.000000000 +0200
+++ pvmnet.c	2015-01-17 14:54:25.000000000 +0100
@@ -94,6 +94,7 @@

 	err = -ENOMEM;
 	dev = alloc_netdev(sizeof(struct pvmnet_priv), "eth%d",
+				 NET_NAME_PREDICTABLE,
 			   pvmnet_setup);
 	if (!dev) {
 		goto err_free_res;
